// TODO: direct unit tests for src/core/path.ts
//
// Planned test cases:
// - nest — converts dotted key + value into nested ConfigDict
// - path traversal — resolving dot-separated paths through nested dicts and arrays
// - edge cases — empty keys, numeric segments for arrays, deeply nested paths
// - findTag — tag tree path traversal

import { describe, it } from "vitest";

describe("path (TODO)", () => {
  it.todo("placeholder for path unit tests");
});
